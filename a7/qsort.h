#include<stdio.h>
#include<stdlib.h>
using namespace std;

#ifdef QSORT_H
#define QSORT_H
int arr[10000];
        for(int i=0;i<1000;i++){
            arr[i]=0 + (rand() % 1000);
        }
        for(int i=0;i<1000;i++){
				cout<<arr[i]<<","<<" ";
		}
		cout<<endl;

#endif